1 - Syntax Error - Invalid syn-if for pattern-assign/while
if ifs;
Select ifs pattern ifs(_, _)

5000
2 - Syntax Error - Invalid syn-while for pattern-if
while w;
Select w pattern w(_, _, _)

5000
3 - Semantic Error - Invalid entRef for pattern-assign
assign a; if ifs;
Select a pattern a(ifs, _)

5000
4 - Semantic Error - Invalid entRef for pattern-assign
assign a; while w;
Select a pattern a(w, _)

5000